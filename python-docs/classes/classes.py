def classes():
    """data, function = class

    new class = new obj / instance
    obj - attributes
        - methods to modify state

    Python class has std features of OOP:
    inheritance (many base classes), override (methods) in derived class, method can call base class method

    like module, created at run-time"""
    pass
